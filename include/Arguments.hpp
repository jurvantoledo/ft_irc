#ifndef ARGUMENTS_H
#define ARGUMENTS_H

#include <iostream>
#include <string>

class Arguments
{
    private:
        std::string _packet;
    public:
        Arguments(/* args */);
        ~Arguments();
};

#endif